# 기술 규칙

- **순수 HTML/CSS/JS**: 프레임워크, 빌드 도구(번들러/트랜스파일러), 외부 패키지 매니저(npm 등) 없이 순수 HTML/CSS/JS로만 구현한다.
- **파일 구성 고정**: 코드 파일은 `index.html`, `style.css`, `app.js` 3개만 유지한다. 파일을 추가로 분리하지 않는다.
- **데이터 저장**: 데이터는 서버/외부 API 없이 브라우저 `localStorage`에 저장한다.

# 작업 규칙

- **한 번에 한 태스크만**: 여러 기능/수정 사항을 한 번에 묶어서 진행하지 않고, 하나의 태스크를 완료·확인한 뒤 다음 태스크로 넘어간다.
- **변경 후 반드시 브라우저에서 확인**: 코드를 변경하면 실제로 브라우저에서 열어 동작을 확인한 뒤에 작업을 완료로 간주한다. 코드 검토만으로 완료 처리하지 않는다.

# 참조 문서

- 요구사항은 [PRD.md](./PRD.md)를 기준으로 한다.
