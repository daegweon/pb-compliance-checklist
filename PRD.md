# PRD: 증권사 직원용 고객 상담 컴플라이언스 체크리스트 & 메모장

## 결론 (요약)
증권사 영업점 직원(PB/RM)이 고객 상담 시 상품별 필수 고지사항을 빠짐없이 안내하고, 상담 내용을 기록·보관할 수 있도록 돕는 웹 도구. 상품별 고지 스크립트는 정적 데이터로 앱 내에 미리 정의하고, 사용자가 입력하는 체크리스트·메모는 **Supabase(Postgres)**에 저장해 기기 간에도 동일한 상담 목록을 조회할 수 있다. (2026-08-12 기준: 기존 `localStorage` 방식에서 Supabase 방식으로 전환)

2026-08-12 기능 고도화: 상담 세션에 상태(진행중/완료/취소) 개념을 추가하고, 체크리스트·상태 변경을 감사 로그로 남겨 컴플라이언스 대응력을 높였다. 대시보드에는 상태뱃지·체크리스트 완료율·상태 필터를 추가했고, 상담 요약을 인쇄(PDF 저장 겸용)할 수 있게 했다. 또한 PB가 상품유형별로 자신만의 고지 스크립트를 직접 등록·삭제할 수 있는 기능을 라이브러리 화면에 추가했다. 이어서 대시보드에 상담 내역 삭제 기능을, 랜딩페이지에는 스크롤 진입 애니메이션을 추가했고, 다크모드 토글과 한글 지원 폰트(Pretendard)를 적용했다. 대시보드에 요약 통계(오늘 상담·이번 주 완료율·상품유형별 분포)와 CSV 내보내기 기능을 추가했다. 상담 진행 화면의 완료/취소 버튼을 화면 하단으로 옮기고 "완료"는 저장 후 목록으로 이동, "취소"는 저장 없이 목록으로만 이동하도록 동작을 재정의했으며, 대시보드 목록에서도 체크박스로 완료 여부를 바로 토글할 수 있게 했다. Supabase Auth 기반 로그인을 도입해, 상담 세션은 작성한 본인만 조회·수정·삭제할 수 있도록 RLS로 사용자별 접근을 제어했다(고지 스크립트 라이브러리는 로그인한 모든 사용자가 함께 쓰는 공용 자료로 유지). 이후 이메일/비밀번호 회원가입은 Supabase 기본 메일 발송 한도 문제로 비활성화하고, Google OAuth 로그인으로 전환했다. 헤더의 로그인/로그아웃 버튼 위치를 다크모드 토글 기준 한쪽으로 통일했고, 대시보드에 "체크리스트 미이행 경보"를 추가해 완료 처리된 상담인데 체크리스트가 100% 채워지지 않은 건을 요약 통계 카드와 목록 행(경고 배지)에서 강조 표시하도록 했다. 상담 화면의 "변경 이력" 패널에는 검색어(이력 내용)·항목 유형(체크리스트 변경/상태 변경/세션 생성) 필터를 추가해, 이력이 많이 쌓여도 원하는 변경 건을 빠르게 찾을 수 있게 했다. 스크립트 라이브러리 화면 진입 시 상품유형별 패널과 "나만의 스크립트 목록"이 각자 `custom_scripts`를 중복 조회하던 것을 한 번만 조회해 나눠 쓰도록 바꿔 로딩 시간을 단축했다. 이어서 PB 커스텀 스크립트를 로그인한 모든 사용자가 함께 보는 공용 자료에서 본인(user_id = auth.uid())만 조회·수정·삭제할 수 있는 사용자별 자료로 전환했다(기존에 소유자 정보 없이 등록돼 있던 공용 스크립트는 전환 과정에서 모두 삭제됨).

---

## 1. 목표 (Goal)

- 상담 중 상품별 필수 설명·위험 고지 항목을 놓치지 않도록 체크리스트로 안내하여 불완전판매 리스크를 줄인다.
- 상담 내용을 상담 세션 단위로 기록해두어, 이후 고객 문의·민원 대응이나 후속 상담 시 참고할 수 있게 한다.
- 신입/저연차 직원이 상품별 필수 고지 문구를 매번 암기하지 않고도 참고할 수 있는 표준 스크립트를 제공한다.

## 2. 사용자 (Target Users)

| 구분 | 설명 |
|---|---|
| 주요 사용자 | 증권사 영업점 창구 직원, PB(프라이빗뱅커), RM(고객관리 담당자) |
| 특징 | 하루 여러 건의 대면/전화 상담을 처리하며, 상품별 고지 의무를 정확히 이행해야 하는 컴플라이언스 부담이 있음 |
| 비대상 사용자 | 트레이딩 데스크(실시간 시세 기반 매매 담당자), 리서치 애널리스트 |

## 3. 핵심 기능 (Core Features, 3개)

| # | 기능명 | 설명 |
|---|---|---|
| 1 | 상담 체크리스트 | 상품유형(펀드/ELS/채권/신탁 등) 선택 시 해당 상품의 필수 확인·고지 항목이 체크리스트로 표시됨. 항목 체크 시 상담 세션에 진행 상태로 저장 |
| 2 | 상담 메모장 | 상담 세션별로 자유 텍스트 메모를 작성·저장. 고객명(또는 별칭)·상담일시와 함께 기록되어 이후 목록에서 검색·열람 가능 |
| 3 | 상품별 고지 스크립트 참고자료 | 상품유형별로 미리 정의된 필수 고지 문구(정적 텍스트)를 상담 화면에서 바로 열람 가능. 실제 낭독·안내 시 참고용 |

## 4. 화면 구성 (Screen Composition)

| 화면 | 주요 구성 요소 |
|---|---|
| 로그인 화면 | "Google로 로그인" 버튼, 오류 메시지 표시 |
| 상담 목록(대시보드) 화면 | 요약 통계 카드(오늘 상담·이번 주 완료율·진행중 건수·상품유형별 분포), 본인이 작성한 상담 세션 목록(날짜·고객명·상품유형·완료 체크박스·상태뱃지·체크리스트 완료율), 새 상담 시작 버튼, CSV 내보내기 버튼, 고객명 검색 + 상태(진행중/완료/취소) 필터, 세션별 삭제 버튼 |
| 상담 진행 화면 | 상품유형 선택, 해당 상품 체크리스트, 고지 스크립트 참고 패널(PB 등록 스크립트 포함), 메모 입력창, 변경 이력(감사 로그) 패널, 상담 요약 인쇄 버튼, 화면 하단의 취소/완료 버튼 |
| 상품 스크립트 라이브러리 화면 | 상품유형별 필수 고지 스크립트 전체 목록을 상담과 무관하게 열람 가능한 참고 화면 + 본인만 보이는 나만의 스크립트 목록/등록 폼 |
| 헤더 | 로그인 시 사용자 이메일·로그아웃 버튼 표시, 다크모드 토글 |

## 5. 제외 범위 (Out of Scope)

- 실시간 시세, 실제 상품 정보 등 외부 API 연동 — 상품 고지 스크립트는 모두 정적 텍스트로 앱 내에 미리 정의
- 실제 고객관리시스템(CRM)·사내 시스템 연동 — Supabase 하나만 사용하는 독립적인 도구로 한정
- 전자서명, 법적 효력이 있는 상담확인서 발급 기능 — 참고·기록용 메모 도구로 한정
- 완전 오프라인 동작 — Supabase 전환 이후로는 인터넷 연결이 없으면 상담 목록 조회·저장이 불가능하다 (이전 `localStorage` 버전과의 차이점)
- 이메일/비밀번호 가입, 다른 소셜 로그인, 비밀번호 재설정 화면, 관리자용 사용자 관리 기능 — Google 계정 로그인만 지원하며, 계정 관리는 Supabase/Google 대시보드에서 수동으로 처리한다

## 6. 검증 기준 (Acceptance Criteria)

| 항목 | 기준 |
|---|---|
| 체크리스트 정확성 | 선택한 상품유형에 맞는 필수 고지 항목이 누락 없이 표시됨 |
| 데이터 보존 | 브라우저를 새로고침하거나 다른 기기·브라우저로 접속해도 Supabase에 저장된 체크리스트 상태·메모가 그대로 복원됨 |
| 세션 관리 | 새 상담 세션 생성 시 기존 세션 데이터가 덮어써지지 않고 Supabase `consultation_sessions` 테이블에 별도 행으로 누적됨 |
| 스크립트 열람 | 상담 화면과 라이브러리 화면 모두에서 상품유형별 고지 스크립트 전문(내장 스크립트 + PB 등록 스크립트)이 정상적으로 표시됨 |
| 네트워크 장애 대응 | Supabase 연결 실패 시 화면이 깨지지 않고 "서버에 연결할 수 없습니다" 등 안내 문구를 표시함 (완전 오프라인 동작은 더 이상 보장하지 않음) |
| 상태 관리 | 상담 화면 하단의 "완료" 클릭 시 입력 중이던 내용이 저장되고 상태가 완료로 바뀐 뒤 대시보드로 이동함. "취소" 클릭 시에는 저장 없이 대시보드로만 이동함(상태·미저장 입력 모두 변경되지 않음). 대시보드 목록의 완료 체크박스로도 완료/진행중 상태를 즉시 전환할 수 있음 |
| 감사 로그 | 체크리스트 체크/해제, 상태 변경, 세션 생성 시점에 `session_audit_log`에 이력이 기록되고, 상담 화면의 "변경 이력" 패널에서 시간순으로 조회됨 |
| 완료율 표시 | 대시보드의 각 세션 행에 해당 상품유형 체크리스트 기준 완료 항목 수(예: 3/8)가 정확히 표시됨 |
| 인쇄 출력 | 상담 화면에서 "인쇄" 클릭 시 고객명·상품유형·상태·체크리스트·메모가 담긴 인쇄 미리보기가 열리고, 앱의 다른 UI 요소는 인쇄물에 포함되지 않음 |
| 커스텀 스크립트 등록 | 라이브러리 화면에서 상품유형·제목·내용을 입력해 등록하면 목록에 즉시 나타나고, 삭제 시 목록과 상담 화면 스크립트 패널에서 함께 사라짐 |
| 상담 내역 삭제 | 대시보드에서 "삭제" 클릭 시 확인 절차 후 세션과 연결된 변경 이력이 함께 삭제되고, 목록에서 즉시 사라짐 |
| 스크롤 애니메이션 | 랜딩페이지에서 기능 카드·클로징 섹션이 뷰포트에 들어올 때 fade-in/slide-up 효과로 나타남 |
| 요약 통계 | 대시보드 상단 카드에 오늘 생성된 상담 건수, 이번 주(월요일~오늘) 완료율(계산식: 완료 건수/이번 주 생성 건수 × 100), 진행중 건수, 상품유형별 분포가 전체 세션 기준으로 정확히 표시됨 |
| CSV 내보내기 | 대시보드에서 "CSV 내보내기" 클릭 시 현재 검색어·상태 필터가 적용된 목록만 CSV로 다운로드되고, 엑셀에서 열었을 때 한글이 깨지지 않음 |
| 로그인 | "Google로 로그인" 클릭 시 Google 인증 화면으로 이동하고, 인증 성공 후 앱으로 돌아오면 대시보드로 이동함. 인증 실패/거부 시 오류 메시지가 표시됨 |
| 라우팅 가드 | 로그인하지 않은 상태에서 대시보드/상담/라이브러리 화면에 접근하면 로그인 화면으로 이동하며, 로그인 후 다시 접근하면 정상적으로 화면이 표시됨 |
| 사용자별 데이터 격리 | 서로 다른 계정으로 로그인했을 때 각자 본인이 작성한 상담 세션만 목록에 보이고, 다른 사용자의 세션 id로 직접 접근해도 조회되지 않음(RLS로 DB 단에서도 차단됨) |
| 사용자별 커스텀 스크립트 | 내가 등록한 커스텀 스크립트는 라이브러리 화면과 상담 화면 스크립트 패널에서 나에게만 보이고, 다른 계정으로 로그인하면 보이지 않음(RLS로 DB 단에서도 차단됨) |
| 로그아웃 | 헤더의 "로그아웃" 클릭 시 세션이 종료되고 랜딩 화면으로 이동하며, 이후 보호된 화면 접근 시 다시 로그인 화면으로 이동함 |
| 체크리스트 미이행 경보 | 상태가 "완료"인데 해당 상품유형 체크리스트가 100% 채워지지 않은 세션은 대시보드 요약 통계에 "체크리스트 미이행 경보" 건수로 집계되고, 목록에서도 해당 행에 경고 배지가 표시됨. 상태가 "진행중"·"취소"인 세션이나 체크리스트가 모두 채워진 "완료" 세션에는 경보가 표시되지 않음 |
| 변경 이력 검색·필터 | 상담 화면의 "변경 이력" 패널에서 검색어를 입력하면 해당 텍스트가 포함된 이력만 남고, 항목 유형(체크리스트 변경/상태 변경/세션 생성)을 선택하면 그 유형의 이력만 표시됨. 조건에 맞는 이력이 없으면 "검색 결과가 없습니다" 문구가 표시되고, 다른 세션으로 이동하면 검색어·필터가 초기화됨 |
